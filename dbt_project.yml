name: motor_claims_warehouse
version: 1.0.0
config-version: 2

profile: motor_claims_warehouse
model-paths: ["models"]
test-paths: ["tests_dbt"]
clean-targets: ["target", "dbt_packages"]

models:
  motor_claims_warehouse:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: ephemeral
    marts:
      +schema: marts
      +materialized: table
    reporting:
      +schema: reporting
      +materialized: view
